<h1><%= @menu.name %></h1>
<ul>
	<% @menu.pages.each do |page| %>
	<li><%= link_to page.title, admin_page_path(page) %></li>
	<% end %>
	</ul>
<p>Menu created: <%= @menu.created_at.strftime '%d. %m. %Y at %T UTC' %> | Last updated: 
	<%= @menu.updated_at.strftime '%d. %m. %Y at %T UTC' %></p>